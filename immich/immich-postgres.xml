<?xml version="1.0" encoding="utf-8" ?>
<Container version="2">
  <Name>immich-postgres</Name>
  <Repository>ghcr.io/immich-app/postgres:14-vectorchord0.4.3-pgvectors0.2.0</Repository>
  <Registry>https://github.com/immich-app/base-images/pkgs/container/postgres</Registry>
  <Network>immich</Network>
  <Support>https://github.com/immich-app/immich/discussions</Support>
  <Project>https://www.postgresql.org/</Project>
  <GitHub>https://github.com/postgres/postgres</GitHub>
  <Overview>PostgreSQL 14 database for Immich, pre-built with the VectorChord / pgvecto.rs vector extensions that Immich requires. This is the "database" service from the official Immich docker-compose.yml. It must be on the same custom Docker network ("immich") as the Immich server so the server can reach it by the container name "immich-postgres". POSTGRES_PASSWORD here must match DB_PASSWORD in the immich-server container. NOTE: changing the username/database/password after first start has no effect because the data directory is already initialized.</Overview>
  <Category>Tools: Other:</Category>
  <WebUI/>
  <TemplateURL>https://raw.githubusercontent.com/sgraaf/Unraid-Docker-Templates/main/immich/immich-postgres.xml</TemplateURL>
  <Icon>https://raw.githubusercontent.com/sgraaf/Unraid-Docker-Templates/main/immich/immich-postgres.png</Icon>
  <ExtraParams>--shm-size=128m</ExtraParams>
  <PostArgs/>
  <Config Name="Database password" Target="POSTGRES_PASSWORD" Default="" Mode="" Description="Password for the Postgres database. MUST match DB_PASSWORD set in the immich-server container. Use only A-Za-z0-9 characters." Type="Variable" Display="always" Required="true" Mask="true"/>
  <Config Name="Database user" Target="POSTGRES_USER" Default="postgres" Mode="" Description="Postgres username. MUST match DB_USERNAME in immich-server." Type="Variable" Display="always" Required="true" Mask="false">postgres</Config>
  <Config Name="Database name" Target="POSTGRES_DB" Default="immich" Mode="" Description="Postgres database name. MUST match DB_DATABASE_NAME in immich-server." Type="Variable" Display="always" Required="true" Mask="false">immich</Config>
  <Config Name="Init args" Target="POSTGRES_INITDB_ARGS" Default="--data-checksums" Mode="" Description="Arguments passed to initdb on first start. Leave as --data-checksums." Type="Variable" Display="advanced" Required="false" Mask="false">--data-checksums</Config>
  <Config Name="DB storage type (HDD)" Target="DB_STORAGE_TYPE" Default="" Mode="" Description="Optional. Set to HDD if the database data is NOT stored on an SSD. Leave blank for SSD." Type="Variable" Display="advanced-hide" Required="false" Mask="false"/>
  <Config Name="Database data" Target="/var/lib/postgresql/data" Default="/mnt/user/appdata/immich/postgres" Mode="rw" Description="Postgres data directory. Equivalent to DB_DATA_LOCATION in the compose .env file. Keep this on fast/SSD storage. Do NOT use a path that is also handled by another mover/backup process that locks files." Type="Path" Display="always" Required="true" Mask="false">/mnt/user/appdata/immich/postgres</Config>
</Container>
